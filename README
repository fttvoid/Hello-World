1. Create the README file

mkdir ~/Hello-World
cd ~/Hello-World
git init
touch README


2. Commit your README

git add README
git commit -m 'Ã¹¹øÂ°commit'

git remote add origin git@github.com:Hello-World.git
git push origin master